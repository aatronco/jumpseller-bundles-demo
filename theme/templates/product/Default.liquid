{{ index_for_top_components }}

{% render 'theme_breadcrumbs' %}

{% unless options.pb_quickview %}
  <link rel="stylesheet" href="{{ 'component-product.css' | asset }}">
{% endunless %}

<main>
  {{ index_for_components }}
</main>

{{ index_for_bottom_components }}
